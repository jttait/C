#include <assert.h>
#include <string.h>
#include <stdio.h>
#include <stdbool.h>
#include <math.h>
#include "sol.h"

#define swap(t, x, y) { t temp; temp = x; x = y; y = temp; }

int main()
{
   return 0;
}
